<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="attachment_form_view_inherit" model="ir.ui.view">
        <field name="name">attachment.form.view.inherit</field>
        <field name="model">ir.attachment</field>
        <field name="inherit_id" ref="base.view_attachment_form" />
        <field name="arch" type="xml">
            <field name="type" position="before">
                <field name="directory_id" context="{'default_directory_id': directory_id}" />
                <field name="document_tags" widget="many2many_tags" options="{'color_field': 'color', 'no_create_edit': True}" />
                <field name="sh_user_ids" widget="many2many_tags" />
                <field name="sh_share_url" widget="CopyClipboardChar" readonly="1" invisible="1" />
            </field>
            <xpath expr="//group" position="before">
                <div class="row">
                    <div class="col-11" />
                    <div class="col-1">
                        <button type="object" name="action_share_directory" class="btn btn-primary sh_share_btn" icon="fa-share-alt" title="Share" />
                    </div>
                </div>
            </xpath>
        </field>
    </record>
    <record model="ir.ui.view" id="sh_document_management_kanban_view">
        <field name="name">ir.attachment</field>
        <field name="model">ir.attachment</field>
        <field name="priority">1000000</field>
        <field name="arch" type="xml">
            <kanban js_class="sh_documents_kanban">
                <field name="id"/>
                <field name="mimetype"/>
                <field name="type"/>
                <field name="create_uid"/>
                <field name="url"/>
                <field name="create_date"/>
                <field name="name"/>
                <templates>
                    <t t-name="kanban-menu">
                        <a role="menuitem" class="dropdown-item" t-att-href="'/web/content/' + record.id.raw_value + '?download=true'">Download</a>
                        <a t-if="widget.deletable" role="menuitem" type="delete" class="dropdown-item">Delete</a>
                    </t>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_area oe_kanban_global_click o_kanban_attachment">
                            <div class="o_kanban_image">
                                <t t-set="binaryPreviewable"
                                   t-value="new RegExp('(image|video|application/pdf|text)').test(record.mimetype.value) &amp;&amp; record.type.raw_value === 'binary'"/>
                                <div t-attf-class="o_kanban_image_wrapper #{(webimage or binaryPreviewable) ? 'o_kanban_previewer' : ''}">
                                    <t t-set="webimage" t-value="new RegExp('image.*(gif|jpeg|jpg|png|webp)').test(record.mimetype.value)"/>
                                    <div t-if="record.type.raw_value == 'url'" class="o_url_image fa fa-link fa-3x text-muted" aria-label="Image is a link"/>
                                    <img t-elif="webimage" t-attf-src="/web/image/#{record.id.raw_value}" width="100" height="100" alt="Document" class="o_attachment_image"/>
                                    <div t-else="" class="o_image o_image_thumbnail" t-att-data-mimetype="record.mimetype.value"/>
                                </div>
                            </div>
                            <div class="o_kanban_details">
                                <div class="o_kanban_details_wrapper">
                                    <div t-att-title="record.name.raw_value" class="o_kanban_record_title">
                                        <field name="name" class="o_text_overflow"/>
                                    </div>
                                    <div class="o_kanban_record_body">
                                        <t t-if="record.type.raw_value == 'url'">
                                            <span class="o_document_url"><i class="fa fa-globe" aria-label="Document url"/> <field name="url" widget="url"/></span>
                                        </t>
                                        <samp t-else="" class="text-muted"> </samp>
                                    </div>
                                    <div class="o_kanban_record_bottom">
                                        <time class="oe_kanban_bottom_left">
                                            <field name="create_date" widget="date"/>
                                        </time>
                                        <div class="oe_kanban_bottom_right">
                                            <field name="create_uid" widget="many2one_avatar_user"/>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    <record id="sh_document_management_tree_view" model="ir.ui.view">
        <field name="name">ir.attachment</field>
        <field name="model">ir.attachment</field>
        <field name="inherit_id" ref="base.view_attachment_tree"/>
        <field name="arch" type="xml">
            <field name="create_date" position="after">
                <button name="action_document_preview" string="Preview" class="btn btn-primary" type="object"/>
            </field>
        </field>
    </record>
</odoo>